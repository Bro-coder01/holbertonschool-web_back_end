#!/usr/bin/env python3
"""Module that contains a function to compute the floor of a number."""
import math


def floor(n: float) -> int:
    """Return the floor of the given float as an integer."""
    return math.floor(n)
